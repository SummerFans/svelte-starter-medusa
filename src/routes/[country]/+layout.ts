// import { Locales } from '$lib/constants';
// import { setLocale } from '$lib/paraglide/runtime';
// import { country, locale } from '$lib/stores/common';
import type { LayoutLoad } from './$types';

export const load: LayoutLoad = ({ params }) => {

  // init paraglide locale
  // const _locale: any = Locales.get(params.country.toUpperCase() as string)?.toLocaleLowerCase() as string
  // setLocale(_locale)
  // country.set(params.country)
  // locale.set(Locales.get(params.country.toUpperCase() as string) as string)

  return {
    //   country
  }
}